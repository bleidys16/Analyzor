# URL del proxy de IA (worker/ en Cloudflare). Sin esta variable la IA queda desactivada
# y el chat responde con el motor de reglas local.
# En local (wrangler dev): http://localhost:8787
VITE_LLM_URL=
